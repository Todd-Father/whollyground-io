.DS_Store
node_modules/
.vercel/

# Claude: track shared config (settings.json, commands/), ignore machine-specific state
.claude/settings.local.json
.claude/worktrees/
.claude/launch.json
.claude/skills/

# Secrets — this is a PUBLIC repo. Never commit credentials.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*secret*
*credential*
# SendGrid / Zoho / Ghost tokens live in env only, never in tracked files.
